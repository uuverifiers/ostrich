(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "aaaa")) (str.to.re "bbbb")))))
(assert (= (str.len x) 8))
(assert (not (= x "bbbbbbbb")))
(assert (not (= x "aaaabbbb")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "$")) (str.to.re "l")))))
(assert (= (str.len x) 3))
(assert (not (= x "$$l")))
(assert (not (= x "$ll")))
(assert (not (= x "l$l")))
(check-sat)
(get-model)

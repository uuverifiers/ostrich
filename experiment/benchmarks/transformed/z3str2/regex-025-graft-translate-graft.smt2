(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (re.++ (re.* (str.to.re "*")) (re.* (str.to.re "*")))) (str.to.re "j")))))
(assert (str.in.re y (str.to.re "j")))
(assert (= 7 (str.len x)))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

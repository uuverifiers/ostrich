(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.* (str.to.re "")) (str.to.re "b")))))
(assert (str.in.re y (re.* (re.union (re.+ (str.to.re "j")) (str.to.re "e")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 6))
(check-sat)
(get-model)

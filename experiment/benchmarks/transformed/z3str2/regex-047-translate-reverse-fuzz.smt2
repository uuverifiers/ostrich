(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "y") (re.++ aRegex (str.to.re "y"))))
(assert (str.in.re key (QuoteRegex (re.+ (str.to.re "w")))))
(assert (= (str.len key) 0))
(check-sat)
(get-model)

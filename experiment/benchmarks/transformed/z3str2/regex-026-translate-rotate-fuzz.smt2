(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.+ (str.to.re "U")) (str.to.re "E")))))
(assert (str.in.re y (re.* (re.union (re.* (str.to.re "V")) (str.to.re "")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

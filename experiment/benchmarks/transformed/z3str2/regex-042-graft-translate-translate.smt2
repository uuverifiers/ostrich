(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "/") (re.* (str.to.re "z")))))
(assert (str.in.re x (re.++ (str.to.re "/") (re.++ (re.* (str.to.re "z")) (str.to.re "g")))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

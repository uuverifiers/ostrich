(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (re.++ (re.* (str.to.re "O")) (str.to.re "z"))) (str.to.re "z")))))
(assert (str.in.re y (re.* (str.to.re "O"))))
(assert (= (str.len x) 7))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

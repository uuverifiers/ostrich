(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re ",")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.++ (str.to.re "p") (re.+ (str.to.re "b"))))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.len y)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re ".LN(") (re.* (str.to.re "K^Y"))))))
(assert (str.in.re y (re.* (re.++ (str.to.re "b(~") (re.+ (str.to.re "bD"))))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 10))
(check-sat)
(get-model)

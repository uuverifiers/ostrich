(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "==") (re.* (str.to.re "ll")))))
(assert (str.in.re x (re.++ (str.to.re "ww") (re.* (str.to.re "ww")))))
(assert (str.in.re x (re.++ (re.* (re.* (str.to.re "ww"))) (re.++ (str.to.re "==") (str.to.re "ll")))))
(check-sat)
(get-model)

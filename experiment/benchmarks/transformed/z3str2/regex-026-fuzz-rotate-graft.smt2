(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.+ (str.to.re "K")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.union (str.to.re "%") (str.to.re "5")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "W")) (str.to.re "I"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "I")) (re.* (re.++ (str.to.re "&") (str.to.re "W"))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

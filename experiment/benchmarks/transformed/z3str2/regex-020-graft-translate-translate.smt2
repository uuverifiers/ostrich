(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "#") (re.* (str.to.re "D"))))))
(assert (= 3 (str.len x)))
(assert (not (= x "D##")))
(assert (not (= x "#D#")))
(assert (not (= x "DD#")))
(check-sat)
(get-model)

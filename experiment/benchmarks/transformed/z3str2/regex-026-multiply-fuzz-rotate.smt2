(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "bb")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.union (re.+ (str.to.re "b")) (str.to.re "Ur")))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
(get-model)

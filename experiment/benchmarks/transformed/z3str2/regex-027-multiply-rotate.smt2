(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "bb")) (str.to.re "aa")))))
(assert (= (str.len x) 6))
(assert (not (= x "aaaabb")))
(assert (not (= x "aabbbb")))
(assert (not (= x "bbaabb")))
(check-sat)
(get-model)

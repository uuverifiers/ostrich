(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "ff") (re.+ (str.to.re "aa"))))))
(assert (str.in.re y (re.+ (re.union (str.to.re "MM") (re.+ (str.to.re ""))))))
(assert (= (str.to.int x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 4))
(check-sat)
(get-model)

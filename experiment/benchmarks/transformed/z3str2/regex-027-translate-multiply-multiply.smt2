(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "YYYY")) (str.to.re "LLLL")))))
(assert (= (str.len x) 12))
(assert (not (= x "YYYYYYYYLLLL")))
(assert (not (= x "YYYYLLLLLLLL")))
(assert (not (= x "LLLLYYYYLLLL")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (= (str.++ x y) (str.++ m n)))
(assert (str.in.re n (str.to.re "zzaaUU")))
(assert (> (str.len x) (str.len m)))
(assert (= 6 (str.len n)))
(assert (= (str.len y) 2))
(check-sat)
(get-model)

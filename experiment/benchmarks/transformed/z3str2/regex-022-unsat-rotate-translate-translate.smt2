(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "f")) (str.to.re "{")))))
(assert (= (str.len x) 2))
(assert (not (= x "ff")))
(assert (not (= x "{f")))
(check-sat)
(get-model)

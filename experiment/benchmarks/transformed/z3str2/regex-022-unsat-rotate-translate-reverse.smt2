(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "E") (re.* (str.to.re "J"))))))
(assert (= (str.len x) 2))
(assert (not (= x "JJ")))
(assert (not (= x "JE")))
(check-sat)
(get-model)

(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "}") (re.++ (str.to.re "}") aRegex)))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re ")")))))
(assert (= (str.len key) 2))
(check-sat)
(get-model)

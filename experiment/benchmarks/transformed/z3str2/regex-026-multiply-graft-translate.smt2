(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "II")))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "CC")) (re.* (re.++ (re.* (str.to.re "CC")) (str.to.re "II")))))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

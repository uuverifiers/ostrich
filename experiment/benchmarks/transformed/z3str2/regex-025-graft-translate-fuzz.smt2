(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.+ (re.union (re.* (str.to.re "")) (str.to.re "j"))) (str.to.re "")))))
(assert (str.in.re y (re.* (str.to.re "*"))))
(assert (= (str.to.int x) 5))
(assert (not (= x y)))
(assert (= (str.len x) (str.to.int y)))
(check-sat)
(get-model)

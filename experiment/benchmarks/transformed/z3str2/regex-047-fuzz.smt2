(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.++ (str.to.re "a") aRegex) (str.to.re """")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "S")))))
(assert (= (str.to.int key) 0))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "P") (re.* (str.to.re ""))))))
(assert (= (str.len x) 5))
(assert (not (= x "hba")))
(assert (not (= x "Da")))
(assert (not (= x "b/V")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "")) (str.to.re "2")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "O")) (str.to.re "''\n''")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 0))
(check-sat)
(get-model)

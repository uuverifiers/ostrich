(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "ll")) (str.to.re "$$")))))
(assert (= (str.len x) 6))
(assert (not (= x "$$$$ll")))
(assert (not (= x "$$llll")))
(assert (not (= x "ll$$ll")))
(check-sat)
(get-model)

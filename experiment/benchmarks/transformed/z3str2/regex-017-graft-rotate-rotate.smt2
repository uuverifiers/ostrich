(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.union (str.to.re "AB") (str.to.re "abcd")) (str.to.re "123")))))
(assert (= (str.len x) 5))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "P")) (re.* (str.to.re "P")))))
(assert (str.in.re x (re.++ (str.to.re "L") (re.++ (str.to.re "L") (str.to.re "K")))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

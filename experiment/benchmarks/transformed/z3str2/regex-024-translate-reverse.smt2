(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "Q") (re.* (str.to.re "V"))))))
(assert (str.in.re y (re.* (re.++ (str.to.re "Q") (re.* (str.to.re "V"))))))
(check-sat)
(get-model)

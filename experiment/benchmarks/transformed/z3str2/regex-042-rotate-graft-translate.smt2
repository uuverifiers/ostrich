(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re ";") (re.* (str.to.re "z")))))
(assert (str.in.re x (re.++ (str.to.re ";") (re.++ (re.* (re.* (str.to.re "L"))) (str.to.re "z")))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "LL")) (str.to.re ",,"))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "ss")) (re.* (str.to.re "LL"))) (str.to.re ",,"))))
(assert (= 6 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "W") (re.* (str.to.re "''\n''")))))
(assert (str.in.re x (re.++ (str.to.re "W") (str.to.re "''\n''"))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "v;kh"))))
(assert (str.in.re x (str.to.re "v;khv;kh")))
(assert (> (str.len x) 20))
(assert (< 25 (str.len x)))
(check-sat)
(get-model)

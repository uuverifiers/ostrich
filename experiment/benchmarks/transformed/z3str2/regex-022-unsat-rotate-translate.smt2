(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "J")) (str.to.re "E")))))
(assert (= (str.len x) 2))
(assert (not (= x "JJ")))
(assert (not (= x "EJ")))
(check-sat)
(get-model)

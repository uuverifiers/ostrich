(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.* (str.to.re "2")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "''\n''")) (str.to.re "O")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 0))
(check-sat)
(get-model)

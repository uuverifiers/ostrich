(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "TT") (str.to.re "TT")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "'\n''\n'")) (re.* (str.to.re "'\n''\n'"))))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 14 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "q") (re.* (str.to.re "&"))))))
(assert (= (str.len x) 3))
(assert (not (= x "&qq")))
(assert (not (= x "&&q")))
(assert (not (= x "&q&")))
(check-sat)
(get-model)

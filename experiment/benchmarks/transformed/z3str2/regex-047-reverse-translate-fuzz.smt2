(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "y") (re.union aRegex (str.to.re "|"))))
(assert (str.in.re key (QuoteRegex (re.+ (str.to.re "U")))))
(assert (= (str.to.int key) 1))
(check-sat)
(get-model)

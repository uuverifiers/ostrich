(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "bb")) (str.to.re "oo")))))
(assert (str.in.re y (re.+ (re.++ (re.+ (str.to.re "VV")) (str.to.re "ee")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.len x) 20))
(check-sat)
(get-model)

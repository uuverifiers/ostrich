(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.++ aRegex (re.* (str.to.re "aa"))) (str.to.re """""")))
(assert (str.in.re key (QuoteRegex (str.to.re """"""))))
(assert (= 4 (str.len key)))
(check-sat)
(get-model)

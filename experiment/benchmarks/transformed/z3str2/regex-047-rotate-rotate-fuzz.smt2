(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.union (re.union (str.to.re "t") (str.to.re "")) aRegex))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "")))))
(assert (= (str.to.int key) 4))
(check-sat)
(get-model)

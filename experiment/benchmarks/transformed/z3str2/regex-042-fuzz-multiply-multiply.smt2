(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.union (str.to.re "YYYY") (re.* (str.to.re "bbbb")))))
(assert (str.in.re x (re.++ (str.to.re "") (re.union (re.* (str.to.re "aaaa")) (re.* (str.to.re ""))))))
(assert (= 12 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "b") (str.to.re "")))))
(assert (= 2 (str.len x)))
(assert (not (= x "b")))
(assert (not (= x "&a")))
(check-sat)
(get-model)

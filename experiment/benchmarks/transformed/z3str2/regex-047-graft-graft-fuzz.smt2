(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (str.to.re "a"))
(assert (str.in.re key (QuoteRegex (re.+ (re.++ (str.to.re "") (str.to.re """"))))))
(assert (= (str.to.int key) 3))
(check-sat)
(get-model)

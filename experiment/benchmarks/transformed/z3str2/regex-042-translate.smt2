(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "W") (re.* (str.to.re "'\n'")))))
(assert (str.in.re x (re.++ (str.to.re "W") (re.++ (re.* (str.to.re "'\n'")) (re.* (str.to.re "u"))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

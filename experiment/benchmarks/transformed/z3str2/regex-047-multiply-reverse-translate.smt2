(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "CC") (re.++ aRegex (str.to.re "CC"))))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "cc")))))
(assert (= (str.len key) 4))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "LL") (re.* (str.to.re "PP")))))
(assert (str.in.re x (re.++ (str.to.re "LL") (re.++ (re.* (str.to.re "PP")) (str.to.re "KK")))))
(assert (= (str.len x) 6))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.+ (str.to.re "Ju")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "b")) (str.to.re "r")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 18))
(check-sat)
(get-model)

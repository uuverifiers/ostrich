(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.union (re.++ (str.to.re "") aRegex) (str.to.re """gt")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "My9")))))
(assert (= (str.to.int key) 3))
(check-sat)
(get-model)

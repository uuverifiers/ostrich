(declare-const x String)
(declare-const y String)
(assert (= (str.len x) 12))
(assert (= x y))
(assert (str.in.re y (re.* (re.range "a" "b"))))
(assert (str.suffixof "mJ|)'{P" x))
(check-sat)
(get-model)

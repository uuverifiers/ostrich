(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "")) (str.to.re "b")))))
(assert (str.in.re y (re.* (re.++ (str.to.re "") (str.to.re "b")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 0 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "L")) (str.to.re "s"))))
(assert (str.in.re x (re.++ (str.to.re ",") (re.++ (str.to.re ",") (re.* (re.* (str.to.re "L")))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

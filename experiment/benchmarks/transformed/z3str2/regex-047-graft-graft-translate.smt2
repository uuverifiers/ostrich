(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (str.to.re "K"))
(assert (str.in.re key (QuoteRegex (re.* (re.++ (str.to.re "S") (str.to.re "S"))))))
(assert (= (str.len key) 2))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "v")) (str.to.re "Q")))))
(assert (= (str.len x) 2))
(assert (not (= x "QQ")))
(assert (not (= x "vQ")))
(check-sat)
(get-model)

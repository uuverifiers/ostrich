(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "^") (re.* (str.to.re "h"))))))
(assert (= (str.len x) 3))
(assert (not (= x "^^h")))
(assert (not (= x "^h^")))
(assert (not (= x "^hh")))
(check-sat)
(get-model)

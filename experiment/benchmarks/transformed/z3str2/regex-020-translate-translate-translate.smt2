(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "{")) (str.to.re "L")))))
(assert (= (str.len x) 3))
(assert (not (= x "{LL")))
(assert (not (= x "L{L")))
(assert (not (= x "{{L")))
(check-sat)
(get-model)

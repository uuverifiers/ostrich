(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "a") (re.+ (str.to.re "f"))))))
(assert (str.in.re y (re.+ (re.union (str.to.re "") (re.+ (str.to.re "M"))))))
(assert (= (str.to.int x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 2))
(check-sat)
(get-model)

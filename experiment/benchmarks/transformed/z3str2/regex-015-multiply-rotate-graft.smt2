(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "aabb1122"))))
(assert (str.in.re y (re.* (str.to.re "aabb1122"))))
(assert (= (str.len x) (str.len y)))
(assert (= 8 16))
(check-sat)
(get-model)

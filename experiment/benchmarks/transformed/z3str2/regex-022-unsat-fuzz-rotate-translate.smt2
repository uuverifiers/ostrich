(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "a")) (str.to.re "")))))
(assert (= (str.len x) 2))
(assert (not (= x "a")))
(assert (not (= x "'\n'U")))
(check-sat)
(get-model)

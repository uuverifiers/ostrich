(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "yy") (str.to.re "yy")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "BB")))))
(assert (= 4 (str.len key)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "-vld"))))
(assert (str.in.re y (re.+ (str.to.re "-a"))))
(assert (= (str.len x) 4))
(assert (= (str.to.int y) 3))
(check-sat)
(get-model)

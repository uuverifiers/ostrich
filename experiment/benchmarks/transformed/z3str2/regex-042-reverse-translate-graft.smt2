(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "I")) (re.* (str.to.re "I")))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "&")) (str.to.re "W")) (str.to.re "W"))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

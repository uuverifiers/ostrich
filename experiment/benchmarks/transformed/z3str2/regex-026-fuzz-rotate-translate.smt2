(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.+ (str.to.re "f")) (str.to.re "")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "u")) (str.to.re "5")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

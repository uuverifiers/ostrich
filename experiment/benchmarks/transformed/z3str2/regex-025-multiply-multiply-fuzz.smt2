(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.+ (str.to.re "RgcMmnja")) (str.to.re "bbb")))))
(assert (str.in.re y (re.+ (re.++ (re.* (str.to.re "aaa")) (str.to.re "bi5s")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 13))
(check-sat)
(get-model)

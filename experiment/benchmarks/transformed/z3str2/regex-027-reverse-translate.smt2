(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "m") (re.* (str.to.re "l"))))))
(assert (= (str.len x) 3))
(assert (not (= x "mll")))
(assert (not (= x "mml")))
(assert (not (= x "mlm")))
(check-sat)
(get-model)

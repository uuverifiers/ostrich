(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "mm")) (str.to.re "QQ")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "mm")) (str.to.re "QQ")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 14))
(check-sat)
(get-model)

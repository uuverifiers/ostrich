(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.union (str.to.re "") (re.+ (str.to.re "6")))))
(assert (str.in.re x (re.++ (re.union (str.to.re "|") (str.to.re "H")) (re.* (re.* (str.to.re ""))))))
(assert (= 4 (str.len x)))
(check-sat)
(get-model)

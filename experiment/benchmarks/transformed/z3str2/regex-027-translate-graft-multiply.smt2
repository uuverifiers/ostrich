(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "YY") (str.to.re "LL")))))
(assert (= 6 (str.len x)))
(assert (not (= x "YYYYLL")))
(assert (not (= x "YYLLLL")))
(assert (not (= x "LLYYLL")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "dddd")) (str.to.re ">>>>")))))
(assert (= (str.len x) 8))
(assert (not (= x ">>>>>>>>")))
(assert (not (= x "dddd>>>>")))
(check-sat)
(get-model)

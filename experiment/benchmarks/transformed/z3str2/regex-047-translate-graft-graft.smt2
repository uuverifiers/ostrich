(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "y") (str.to.re "y")))
(assert (str.in.re key (QuoteRegex (str.to.re "B"))))
(assert (= (str.len key) 2))
(check-sat)
(get-model)

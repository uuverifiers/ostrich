(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "ww") (re.* (str.to.re "--"))))))
(assert (= (str.len x) 6))
(assert (not (= x "wwww--")))
(assert (not (= x "ww--ww")))
(assert (not (= x "ww----")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re ",")) (str.to.re "L"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "L")) (re.* (re.++ (str.to.re "s") (str.to.re ","))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

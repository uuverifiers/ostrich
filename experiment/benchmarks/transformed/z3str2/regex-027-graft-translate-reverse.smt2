(declare-const x String)
(declare-const y String)
(assert (str.in.re x (str.to.re "$")))
(assert (= 3 (str.len x)))
(assert (not (= x "$||")))
(assert (not (= x "$$|")))
(assert (not (= x "$|$")))
(check-sat)
(get-model)

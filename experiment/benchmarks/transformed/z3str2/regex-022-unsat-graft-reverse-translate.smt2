(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "'\r'") (str.to.re "' '")))))
(assert (= 2 (str.len x)))
(assert (not (= x "'\r''\r'")))
(assert (not (= x "'\r'' '")))
(check-sat)
(get-model)

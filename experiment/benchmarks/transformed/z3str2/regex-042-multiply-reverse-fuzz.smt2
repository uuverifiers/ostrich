(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "b2v")) (str.to.re "2Ba&"))))
(assert (str.in.re x (re.++ (re.union (re.* (str.to.re "?:c")) (re.+ (str.to.re "b"))) (str.to.re "a"))))
(assert (= 10 (str.len x)))
(check-sat)
(get-model)

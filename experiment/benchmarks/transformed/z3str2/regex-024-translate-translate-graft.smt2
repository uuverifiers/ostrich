(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "L")) (str.to.re ")")))))
(assert (str.in.re y (re.* (re.++ (str.to.re ")") (re.* (str.to.re "L"))))))
(check-sat)
(get-model)

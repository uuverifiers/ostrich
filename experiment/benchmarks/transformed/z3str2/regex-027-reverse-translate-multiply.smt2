(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "mm") (re.* (str.to.re "ll"))))))
(assert (= (str.len x) 6))
(assert (not (= x "mmllll")))
(assert (not (= x "mmmmll")))
(assert (not (= x "mmllmm")))
(check-sat)
(get-model)

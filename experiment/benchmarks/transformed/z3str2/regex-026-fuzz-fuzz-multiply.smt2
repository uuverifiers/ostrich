(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "")) (str.to.re "KK")))))
(assert (str.in.re y (re.* (re.union (re.* (str.to.re "SS")) (str.to.re "%%")))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.len y)))
(check-sat)
(get-model)

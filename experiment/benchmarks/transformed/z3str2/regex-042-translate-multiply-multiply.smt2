(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re ",,,,") (re.* (str.to.re "LLLL")))))
(assert (str.in.re x (re.++ (str.to.re ",,,,") (re.++ (re.* (str.to.re "LLLL")) (re.* (str.to.re "ssss"))))))
(assert (= 12 (str.len x)))
(check-sat)
(get-model)

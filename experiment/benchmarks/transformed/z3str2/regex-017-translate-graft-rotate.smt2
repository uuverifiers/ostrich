(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.union (str.to.re "123") (str.to.re """f")) (str.to.re "Yl%c")))))
(assert (= (str.len x) 5))
(check-sat)
(get-model)

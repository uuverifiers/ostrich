(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.+ (str.to.re "SS")) (str.to.re ".N")))))
(assert (str.in.re y (re.* (re.union (re.+ (str.to.re "S")) (str.to.re ".")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 10))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "'\n'") (str.to.re "Q"))))
(assert (str.in.re x (re.++ (re.++ (re.* (re.* (str.to.re "V"))) (re.* (str.to.re "V"))) (str.to.re "Q"))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

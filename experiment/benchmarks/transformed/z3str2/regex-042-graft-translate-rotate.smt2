(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "P") (re.* (str.to.re "L")))))
(assert (str.in.re x (re.++ (re.* (str.to.re "K")) (re.++ (str.to.re "P") (str.to.re "L")))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (re.++ (re.* (str.to.re "aa")) (str.to.re "bb"))) (str.to.re "bb")))))
(assert (str.in.re y (re.* (str.to.re "aa"))))
(assert (= (str.len x) 14))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

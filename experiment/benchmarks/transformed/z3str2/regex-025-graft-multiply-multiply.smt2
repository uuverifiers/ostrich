(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "bbbb") (str.to.re "bbbb")))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re "aaaa")) (re.* (str.to.re "aaaa"))))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 28 (str.len x)))
(check-sat)
(get-model)

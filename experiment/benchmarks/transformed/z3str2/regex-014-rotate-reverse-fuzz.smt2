(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (str.to.re "bc"))))
(assert (str.in.re y (re.* (str.to.re "b"))))
(assert (= (str.len x) 3))
(assert (= (str.len y) 2))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "a") (re.* (str.to.re "x"))))))
(assert (= (str.len x) 3))
(assert (not (= x "xxa")))
(assert (not (= x "xax")))
(assert (not (= x "xaa")))
(check-sat)
(get-model)

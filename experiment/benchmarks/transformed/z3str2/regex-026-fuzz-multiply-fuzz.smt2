(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.+ (str.to.re "")) (str.to.re "g@K")))))
(assert (str.in.re y (re.+ (re.++ (re.* (str.to.re ":{H")) (str.to.re "H\\")))))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
(get-model)

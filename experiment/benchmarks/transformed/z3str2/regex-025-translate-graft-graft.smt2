(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "["))))
(assert (str.in.re y (re.* (re.++ (re.++ (re.* (re.* (str.to.re "["))) (str.to.re ",")) (str.to.re ",")))))
(assert (= (str.len x) 7))
(assert (not (= x y)))
(assert (= (str.len y) (str.len x)))
(check-sat)
(get-model)

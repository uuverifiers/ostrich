(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (re.++ (re.* (str.to.re "**")) (str.to.re "jj"))) (str.to.re "jj")))))
(assert (str.in.re y (re.* (str.to.re "**"))))
(assert (= (str.len x) 14))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

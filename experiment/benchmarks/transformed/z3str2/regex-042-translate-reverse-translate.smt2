(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "'\t'")) (str.to.re "C"))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "Y")) (re.* (str.to.re "'\t'"))) (str.to.re "C"))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

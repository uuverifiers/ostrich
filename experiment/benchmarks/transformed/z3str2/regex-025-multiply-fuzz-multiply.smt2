(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.+ (str.to.re "")) (str.to.re "JJuu")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "rr")) (str.to.re "bb")))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 36))
(check-sat)
(get-model)

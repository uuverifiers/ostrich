(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (str.to.re "") (re.* (str.to.re "aa"))))))
(assert (= (str.len x) 4))
(assert (not (= x "ll{{aaaa")))
(assert (not (= x "bbbb")))
(assert (not (= x "XX**##aa")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "UU")) (str.to.re "PP"))))
(assert (str.in.re x (re.++ (re.* (str.to.re ">>")) (str.to.re ">>"))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re ">>")) (re.* (str.to.re "UU"))) (str.to.re "PP"))))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "f") (re.+ (str.to.re "a"))))))
(assert (str.in.re y (re.+ (re.union (str.to.re "M") (str.to.re "")))))
(assert (= 2 (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int x)))
(check-sat)
(get-model)

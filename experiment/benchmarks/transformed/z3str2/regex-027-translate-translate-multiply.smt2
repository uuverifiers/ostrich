(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "ZZ")) (str.to.re "EE")))))
(assert (= (str.len x) 6))
(assert (not (= x "ZZZZEE")))
(assert (not (= x "ZZEEEE")))
(assert (not (= x "EEZZEE")))
(check-sat)
(get-model)

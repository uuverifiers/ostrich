(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (str.to.re "") (re.* (str.to.re "a"))))))
(assert (= (str.len x) 2))
(assert (not (= x "aa{l")))
(assert (not (= x "bb")))
(assert (not (= x "a#*X")))
(check-sat)
(get-model)

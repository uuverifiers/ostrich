(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "o~o") (re.* (str.to.re "'\t'")))))
(assert (str.in.re x (re.++ (re.* (re.* (str.to.re "#"))) (re.++ (str.to.re "o~o") (str.to.re "'\t'")))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "M")) (re.+ (re.union (re.+ (str.to.re "b")) (str.to.re ">")))))))
(assert (str.in.re y (str.to.re "a")))
(assert (= (str.to.int x) 6))
(assert (not (= x y)))
(assert (= (str.to.int x) (str.to.int y)))
(check-sat)
(get-model)

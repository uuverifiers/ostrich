(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.union (re.++ (str.to.re "s' 'u'Wp'\r''\x0c'E'") aRegex) (str.to.re "")))
(assert (str.in.re key (QuoteRegex (re.+ (str.to.re "")))))
(assert (= (str.to.int key) 9))
(check-sat)
(get-model)

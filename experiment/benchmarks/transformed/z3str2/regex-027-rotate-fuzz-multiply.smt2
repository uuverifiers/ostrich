(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "")) (str.to.re "PP")))))
(assert (= (str.len x) 10))
(assert (not (= x "aabbhh")))
(assert (not (= x "aaDD")))
(assert (not (= x "VV//bb")))
(check-sat)
(get-model)

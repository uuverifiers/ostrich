(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "vB"))))
(assert (str.in.re y (re.+ (str.to.re ""))))
(assert (= (str.to.int x) 1))
(assert (= (str.len y) 2))
(check-sat)
(get-model)

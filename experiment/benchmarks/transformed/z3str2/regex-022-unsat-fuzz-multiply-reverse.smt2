(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "bb") (re.* (str.to.re ""))))))
(assert (= (str.len x) 4))
(assert (not (= x "bb")))
(assert (not (= x "&&aa")))
(check-sat)
(get-model)

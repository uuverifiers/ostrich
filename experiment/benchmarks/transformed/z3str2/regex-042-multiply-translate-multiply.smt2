(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "ZZZZ") (re.* (str.to.re "nnnn")))))
(assert (str.in.re x (re.++ (str.to.re "ZZZZ") (re.++ (re.* (str.to.re "nnnn")) (re.* (str.to.re "SSSS"))))))
(assert (= 12 (str.len x)))
(check-sat)
(get-model)

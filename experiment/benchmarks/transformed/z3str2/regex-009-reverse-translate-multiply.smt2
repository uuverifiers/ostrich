(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (str.to.re "ddRR**XX"))))
(assert (str.in.re x (re.* (str.to.re "ddRR**XXddRR**XX"))))
(assert (> (str.len x) 40))
(assert (< (str.len x) 50))
(check-sat)
(get-model)

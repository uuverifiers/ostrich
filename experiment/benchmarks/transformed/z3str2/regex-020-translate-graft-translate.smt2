(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "l") (str.to.re """")))))
(assert (= 3 (str.len x)))
(assert (not (= x "l""""")))
(assert (not (= x """l""")))
(assert (not (= x "ll""")))
(check-sat)
(get-model)

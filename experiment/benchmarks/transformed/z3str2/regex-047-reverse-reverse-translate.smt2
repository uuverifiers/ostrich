(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.++ (str.to.re "F") aRegex) (str.to.re "F")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "D")))))
(assert (= (str.len key) 2))
(check-sat)
(get-model)

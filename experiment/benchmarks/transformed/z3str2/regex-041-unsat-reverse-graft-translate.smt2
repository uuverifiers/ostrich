(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "b")) (str.to.re "h"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "x")) (re.* (str.to.re "b")))))
(assert (str.in.re x (re.++ (re.++ (re.* (str.to.re "x")) (str.to.re "x")) (str.to.re "h"))))
(check-sat)
(get-model)

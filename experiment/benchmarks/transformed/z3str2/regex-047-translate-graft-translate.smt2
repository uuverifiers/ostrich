(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "d") (str.to.re "d")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "p")))))
(assert (= 2 (str.len key)))
(check-sat)
(get-model)

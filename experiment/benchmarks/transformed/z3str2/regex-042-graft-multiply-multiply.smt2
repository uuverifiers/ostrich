(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "aaaa") (str.to.re "cccc"))))
(assert (str.in.re x (re.++ (str.to.re "aaaa") (re.++ (re.* (str.to.re "bbbb")) (re.* (re.* (str.to.re "bbbb")))))))
(assert (= (str.len x) 12))
(check-sat)
(get-model)

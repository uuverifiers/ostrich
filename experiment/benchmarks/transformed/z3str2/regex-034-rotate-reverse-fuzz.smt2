(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (= (str.++ y x) (str.++ n m)))
(assert (str.in.re n (re.* (str.to.re "ba"))))
(assert (> (str.len x) (str.to.int m)))
(assert (= 1 (str.len n)))
(assert (= 1 (str.to.int y)))
(check-sat)
(get-model)

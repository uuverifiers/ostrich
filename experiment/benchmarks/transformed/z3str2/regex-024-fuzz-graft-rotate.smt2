(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (re.++ (re.+ (str.to.re "b")) (str.to.re "^"))) (str.to.re "")))))
(assert (str.in.re y (re.* (str.to.re "b"))))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "bb") (str.to.re "bb")))))
(assert (str.in.re y (re.+ (re.++ (re.* (str.to.re "")) (re.* (str.to.re "aa"))))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= 22 (str.len x)))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.+ (str.to.re "")) (str.to.re "cc")))))
(assert (str.in.re y (re.+ (re.union (re.* (str.to.re "55")) (str.to.re "ZZ")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

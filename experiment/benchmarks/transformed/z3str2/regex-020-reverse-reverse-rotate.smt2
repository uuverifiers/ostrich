(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "b")) (str.to.re "a")))))
(assert (= (str.len x) 3))
(assert (not (= x "abb")))
(assert (not (= x "bab")))
(assert (not (= x "aab")))
(check-sat)
(get-model)

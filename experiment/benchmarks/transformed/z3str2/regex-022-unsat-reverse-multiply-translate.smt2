(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "^^") (re.* (str.to.re "aa"))))))
(assert (= (str.len x) 4))
(assert (not (= x "^^^^")))
(assert (not (= x "^^aa")))
(check-sat)
(get-model)

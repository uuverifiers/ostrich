(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "aa") (re.* (str.to.re "bb"))))))
(assert (= (str.len x) 6))
(assert (not (= x "bbaaaa")))
(assert (not (= x "bbbbaa")))
(assert (not (= x "bbaabb")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "p")) (str.to.re "R")))))
(assert (= (str.len x) 2))
(assert (not (= x "RR")))
(assert (not (= x "pR")))
(check-sat)
(get-model)

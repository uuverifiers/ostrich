(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (str.to.re "a") (re.* (str.to.re ""))))))
(assert (= (str.len x) 2))
(assert (not (= x "l{aa")))
(assert (not (= x "bb")))
(assert (not (= x "X*#a")))
(check-sat)
(get-model)

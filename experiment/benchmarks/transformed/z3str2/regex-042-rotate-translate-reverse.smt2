(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re ";")) (str.to.re "''\t''"))))
(assert (str.in.re x (re.++ (re.++ (str.to.re ";") (str.to.re "''\t''")) (re.* (re.* (str.to.re "y"))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

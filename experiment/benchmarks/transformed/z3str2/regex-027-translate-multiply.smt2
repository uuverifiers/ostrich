(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "aa")) (str.to.re "<<")))))
(assert (= (str.len x) 6))
(assert (not (= x "aaaa<<")))
(assert (not (= x "aa<<<<")))
(assert (not (= x "<<aa<<")))
(check-sat)
(get-model)

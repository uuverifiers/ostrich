(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "Y") (re.+ (str.to.re ""))))))
(assert (= (str.to.int x) 6))
(assert (not (= x "}t[''\n''")))
(assert (not (= x "v5''\n''}")))
(assert (not (= x "}''\r''2:")))
(check-sat)
(get-model)

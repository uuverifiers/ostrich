(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "aaaa")) (re.* (str.to.re "aaaa"))))))
(assert (str.in.re y (re.* (re.++ (str.to.re "bbbb") (str.to.re "bbbb")))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(check-sat)
(get-model)

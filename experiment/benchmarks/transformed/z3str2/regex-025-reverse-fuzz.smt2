(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "Y") (re.+ (str.to.re "b"))))))
(assert (str.in.re y (re.+ (re.++ (str.to.re "") (re.* (str.to.re "a"))))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.len x) 1))
(check-sat)
(get-model)

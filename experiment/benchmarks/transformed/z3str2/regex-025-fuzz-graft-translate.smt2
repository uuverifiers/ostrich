(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.* (str.to.re "F")) (str.to.re "L")))))
(assert (str.in.re y (re.+ (re.union (str.to.re "F") (str.to.re "L")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 0 (str.len x)))
(check-sat)
(get-model)

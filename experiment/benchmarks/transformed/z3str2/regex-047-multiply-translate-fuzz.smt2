(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.union (str.to.re "'\n'D") aRegex) (str.to.re "vU")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "'\n'l")))))
(assert (= (str.len key) 1))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "}") (str.to.re "W"))))
(assert (str.in.re x (re.++ (re.* (re.* (re.* (str.to.re "|")))) (re.++ (str.to.re "|") (str.to.re "}")))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

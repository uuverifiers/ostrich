(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.union (re.+ (str.to.re "")) (str.to.re "")))))
(assert (= (str.to.int x) 6))
(assert (not (= x "bb")))
(assert (not (= x "eY,vUI_~")))
(assert (not (= x "n`t")))
(check-sat)
(get-model)

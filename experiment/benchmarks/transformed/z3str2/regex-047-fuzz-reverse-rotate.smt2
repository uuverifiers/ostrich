(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (str.to.re "z") (re.union aRegex (str.to.re """"))))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "a")))))
(assert (= (str.len key) 1))
(check-sat)
(get-model)

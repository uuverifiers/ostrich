(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "xx") (re.* (str.to.re "aa"))))))
(assert (= (str.len x) 6))
(assert (not (= x "xxxxaa")))
(assert (not (= x "xxaaxx")))
(assert (not (= x "xxaaaa")))
(check-sat)
(get-model)

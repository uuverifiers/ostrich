(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.union (re.union aRegex (str.to.re "")) (str.to.re "r")))
(assert (str.in.re key (QuoteRegex (re.* (str.to.re "k")))))
(assert (= (str.to.int key) 2))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "LL") (re.* (str.to.re "YY"))))))
(assert (= (str.len x) 6))
(assert (not (= x "LLYYYY")))
(assert (not (= x "LLLLYY")))
(assert (not (= x "LLYYLL")))
(check-sat)
(get-model)

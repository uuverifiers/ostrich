(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "W") (re.* (str.to.re "I")))))
(assert (str.in.re x (re.++ (str.to.re "W") (re.++ (re.* (str.to.re "I")) (re.* (str.to.re "&"))))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

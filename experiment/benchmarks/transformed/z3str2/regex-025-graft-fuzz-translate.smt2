(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "z") (str.to.re "z")))))
(assert (str.in.re y (re.+ (re.++ (re.* (str.to.re "")) (re.* (str.to.re "P"))))))
(assert (= (str.to.int x) (str.len y)))
(assert (not (= x y)))
(assert (= 11 (str.len x)))
(check-sat)
(get-model)

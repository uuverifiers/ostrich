(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.* (re.union (str.to.re "'\t'") (str.to.re "|")))))
(assert (= 3 (str.len x)))
(assert (not (= x "||'\t'")))
(assert (not (= x "'\t'|'\t'")))
(assert (not (= x "'\t'||")))
(assert (not (= x "|||")))
(assert (not (= x "'\t''\t'|")))
(assert (not (= x "|'\t'|")))
(assert (not (= x "'\t''\t''\t'")))
(check-sat)
(get-model)

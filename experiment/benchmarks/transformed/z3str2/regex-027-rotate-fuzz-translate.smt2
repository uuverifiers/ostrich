(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "")) (str.to.re "'\t'")))))
(assert (= (str.len x) 5))
(assert (not (= x "]PF")))
(assert (not (= x "]M")))
(assert (not (= x "wbP")))
(check-sat)
(get-model)

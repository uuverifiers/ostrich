(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "R") (re.* (str.to.re "p"))))))
(assert (= (str.len x) 2))
(assert (not (= x "RR")))
(assert (not (= x "Rp")))
(check-sat)
(get-model)

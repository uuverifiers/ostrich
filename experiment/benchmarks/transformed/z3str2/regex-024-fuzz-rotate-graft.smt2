(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (re.* (str.to.re "b")) (str.to.re "")))))
(assert (str.in.re y (re.* (re.++ (str.to.re "b") (str.to.re "^")))))
(check-sat)
(get-model)

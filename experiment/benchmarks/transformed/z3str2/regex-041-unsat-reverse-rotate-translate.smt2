(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (re.* (str.to.re "m")) (str.to.re ")"))))
(assert (str.in.re x (re.++ (re.* (str.to.re "T")) (str.to.re "T"))))
(assert (str.in.re x (re.++ (re.* (str.to.re ")")) (re.* (re.++ (str.to.re "T") (str.to.re "m"))))))
(check-sat)
(get-model)

(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.++ aRegex (str.to.re """""")) (re.* (str.to.re "aa"))))
(assert (str.in.re key (QuoteRegex (str.to.re """"""))))
(assert (= 4 (str.len key)))
(check-sat)
(get-model)

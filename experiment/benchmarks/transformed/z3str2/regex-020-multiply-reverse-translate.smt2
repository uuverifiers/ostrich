(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "&&") (re.* (str.to.re "oo"))))))
(assert (= (str.len x) 6))
(assert (not (= x "&&&&oo")))
(assert (not (= x "&&oo&&")))
(assert (not (= x "&&oooo")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.union (re.union (str.to.re "ptsR") (str.to.re "321")) (str.to.re "El")))))
(assert (= (str.len x) 5))
(check-sat)
(get-model)

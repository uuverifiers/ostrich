(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (str.to.re "aaaa"))
(assert (str.in.re key (QuoteRegex (re.* (re.++ (re.++ (str.to.re """""""""") aRegex) (str.to.re """"""""""))))))
(assert (= 8 (str.len key)))
(check-sat)
(get-model)

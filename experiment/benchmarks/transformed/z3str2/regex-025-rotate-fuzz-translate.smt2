(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "'\x0c'")) (str.to.re "z")))))
(assert (str.in.re y (re.+ (re.++ (re.+ (str.to.re "#")) (str.to.re "*")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= (str.len x) 10))
(check-sat)
(get-model)

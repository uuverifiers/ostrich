(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "bb")) (re.* (re.++ (re.* (str.to.re "bb")) (str.to.re "aa")))))))
(assert (str.in.re y (str.to.re "aa")))
(check-sat)
(get-model)

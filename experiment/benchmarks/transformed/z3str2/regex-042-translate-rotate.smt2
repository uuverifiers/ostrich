(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "''\n''") (re.* (str.to.re "W")))))
(assert (str.in.re x (re.++ (re.* (re.* (str.to.re "u"))) (re.++ (str.to.re "''\n''") (str.to.re "W")))))
(assert (= 3 (str.len x)))
(check-sat)
(get-model)

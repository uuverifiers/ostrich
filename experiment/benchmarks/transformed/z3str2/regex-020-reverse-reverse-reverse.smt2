(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "b") (re.* (str.to.re "a"))))))
(assert (= (str.len x) 3))
(assert (not (= x "bba")))
(assert (not (= x "bab")))
(assert (not (= x "baa")))
(check-sat)
(get-model)

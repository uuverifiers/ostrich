(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "D") (str.to.re "D")))))
(assert (str.in.re y (re.* (re.++ (re.* (re.* (str.to.re "I"))) (str.to.re "I")))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= 7 (str.len x)))
(check-sat)
(get-model)

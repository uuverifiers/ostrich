(declare-const x String)
(declare-const y String)
(declare-const m String)
(declare-const n String)
(assert (str.in.re x (re.++ (str.to.re "L") (re.* (str.to.re "P")))))
(assert (str.in.re x (re.++ (str.to.re "L") (re.++ (re.* (str.to.re "P")) (str.to.re "K")))))
(assert (= (str.len x) 3))
(check-sat)
(get-model)

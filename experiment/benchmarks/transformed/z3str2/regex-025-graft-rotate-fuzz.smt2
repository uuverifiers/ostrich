(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "b") (str.to.re "L")))))
(assert (str.in.re y (re.* (re.++ (re.* (re.* (str.to.re "a"))) (str.to.re "")))))
(assert (= (str.len x) (str.to.int y)))
(assert (not (= x y)))
(assert (= 11 (str.len x)))
(check-sat)
(get-model)

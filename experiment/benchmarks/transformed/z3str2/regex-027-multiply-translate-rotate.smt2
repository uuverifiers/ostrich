(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "pp")) (str.to.re "''\t''''\t''")))))
(assert (= (str.len x) 6))
(assert (not (= x "''\t''''\t''''\t''''\t''pp")))
(assert (not (= x "''\t''''\t''pppp")))
(assert (not (= x "pp''\t''''\t''pp")))
(check-sat)
(get-model)

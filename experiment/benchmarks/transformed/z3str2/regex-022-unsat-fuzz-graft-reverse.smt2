(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "")) (str.to.re "b")))))
(assert (= 2 (str.len x)))
(assert (not (= x "b")))
(assert (not (= x "&a")))
(check-sat)
(get-model)

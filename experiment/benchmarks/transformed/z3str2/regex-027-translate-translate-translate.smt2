(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re "'")) (str.to.re "r")))))
(assert (= (str.len x) 3))
(assert (not (= x "''r")))
(assert (not (= x "'rr")))
(assert (not (= x "r'r")))
(check-sat)
(get-model)

(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "") (re.* (str.to.re "`"))))))
(assert (= (str.len x) 4))
(assert (not (= x "b")))
(assert (not (= x "b")))
(check-sat)
(get-model)

(declare-const key String)
(declare-const val String)
(define-fun QuoteRegex ((aRegex (RegEx String))) (RegEx String) (re.++ (re.* (str.to.re "a")) (re.union (str.to.re "z") aRegex)))
(assert (str.in.re key (QuoteRegex (str.to.re """"))))
(assert (= 1 (str.len key)))
(check-sat)
(get-model)

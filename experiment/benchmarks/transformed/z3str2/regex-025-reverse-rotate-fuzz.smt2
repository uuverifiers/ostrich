(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.+ (re.++ (str.to.re "a") (re.+ (str.to.re "x"))))))
(assert (str.in.re y (re.+ (re.union (str.to.re "^") (re.* (str.to.re "b"))))))
(assert (= (str.len x) (str.len y)))
(assert (not (= x y)))
(assert (= (str.to.int x) 14))
(check-sat)
(get-model)

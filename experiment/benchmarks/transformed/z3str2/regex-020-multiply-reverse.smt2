(declare-const x String)
(declare-const y String)
(assert (str.in.re x (re.* (re.++ (str.to.re "bb") (re.* (str.to.re "aa"))))))
(assert (= (str.len x) 6))
(assert (not (= x "bbbbaa")))
(assert (not (= x "bbaabb")))
(assert (not (= x "bbaaaa")))
(check-sat)
(get-model)

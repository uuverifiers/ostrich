(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "aaaaaabbbbbb") re.allchar)))
(assert (not (str.in.re S (re.++ (re.++ (re.++ (str.to.re "aaaaaa") re.allchar) (str.to.re "bbbbbb")) re.allchar))))
(check-sat)
(get-model)

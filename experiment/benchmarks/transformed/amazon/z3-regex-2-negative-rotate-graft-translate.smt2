(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "```rrr") re.allchar)))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "rrr") (str.to.re "```"))))))
(check-sat)
(get-model)

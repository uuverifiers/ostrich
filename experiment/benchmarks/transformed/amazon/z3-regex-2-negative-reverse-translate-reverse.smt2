(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "eeeDDD") re.allchar)))
(assert (not (str.in.re S (re.++ (re.++ (re.++ (str.to.re "eee") re.allchar) (str.to.re "DDD")) re.allchar))))
(check-sat)
(get-model)

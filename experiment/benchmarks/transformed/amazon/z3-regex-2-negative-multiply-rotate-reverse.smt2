(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "bbbbbbaaaaaa"))))
(assert (not (str.in.re S (re.++ (re.++ (re.++ re.allchar (str.to.re "aaaaaa")) (str.to.re "bbbbbb")) re.allchar))))
(check-sat)
(get-model)

(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "aaabbb") re.allchar)))
(assert (not (str.in.re S (re.++ (str.to.re "bbb") (re.++ re.allchar (re.++ re.allchar (str.to.re "aaa")))))))
(check-sat)
(get-model)

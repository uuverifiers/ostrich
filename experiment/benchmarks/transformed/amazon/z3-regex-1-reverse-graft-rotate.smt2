(declare-const S String)
(assert (not (str.in.re S (str.to.re "bbb"))))
(assert (str.in.re S (re.++ (re.++ (str.to.re "bbbaaa") (re.++ re.allchar (str.to.re "aaa"))) (re.++ re.allchar re.allchar))))
(check-sat)
(get-model)
(get-info :reason-unknown)

(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "aaaaaaaaaaaabbbbbbbbbbbb") re.allchar)))
(assert (not (str.in.re S (re.++ (re.++ (re.++ (str.to.re "aaaaaaaaaaaa") re.allchar) (str.to.re "bbbbbbbbbbbb")) re.allchar))))
(check-sat)
(get-model)

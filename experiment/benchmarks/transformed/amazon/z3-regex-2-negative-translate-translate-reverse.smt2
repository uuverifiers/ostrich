(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "ggg'''"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "ggg") (re.++ re.allchar (str.to.re "'''")))))))
(check-sat)
(get-model)

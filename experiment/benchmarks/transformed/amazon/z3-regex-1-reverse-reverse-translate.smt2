(declare-const S String)
(assert (not (str.in.re S (re.++ (str.to.re "bbb(((") re.allchar))))
(assert (str.in.re S (re.++ (re.++ (re.++ (str.to.re "bbb") re.allchar) (str.to.re "(((")) re.allchar)))
(check-sat)
(get-model)
(get-info :reason-unknown)

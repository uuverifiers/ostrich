(declare-const S String)
(assert (str.in.re S (re.++ (str.to.re "^^^eee") re.allchar)))
(assert (not (str.in.re S (re.++ (str.to.re "^^^") (re.++ (re.++ re.allchar (str.to.re "eee")) re.allchar)))))
(check-sat)
(get-model)

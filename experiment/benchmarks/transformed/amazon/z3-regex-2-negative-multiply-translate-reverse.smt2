(declare-const S String)
(assert (str.in.re S (re.++ re.allchar (str.to.re "SSSSSSpppppp"))))
(assert (not (str.in.re S (re.++ re.allchar (re.++ (str.to.re "SSSSSS") (re.++ re.allchar (str.to.re "pppppp")))))))
(check-sat)
(get-model)

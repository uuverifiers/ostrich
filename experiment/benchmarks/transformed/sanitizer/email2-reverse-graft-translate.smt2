(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email (str.++ "0" Username)))
(assert (str.in.re Email (re.++ (re.++ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.+ (re.++ (str.to.re "/") (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9"))))))) (str.to.re "q")) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range (str.++ Domainname "q") "9")))))))
(assert (not (str.in.re Domainname (re.++ (str.to.re "/") (re.* re.allchar)))))
(check-sat)

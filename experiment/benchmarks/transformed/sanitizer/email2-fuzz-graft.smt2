(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= 0 24))
(assert (> (str.to.int Username) (str.len Username)))
(assert (<= (str.len Domainname) 88))
(assert (> (str.len Domainname) 0))
(assert (= Email "9"))
(assert (str.in.re Email (re.union (re.* (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "@") (re.++ (re.+ (re.union (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re ""))) (re.* (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" (str.++ Username (str.++ "@" Domainname)))))))))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (re.union (re.* re.allchar) (str.to.re "."))))))
(check-sat)

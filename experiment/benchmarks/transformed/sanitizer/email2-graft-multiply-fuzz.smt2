(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 117))
(assert (> (str.to.int Username) 0))
(assert (<= (str.len Domainname) 471))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.* (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "tPC") (re.union (re.+ (re.union (re.+ (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "?' '") (re.+ re.allchar)))) (re.* (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" (str.++ Username (str.++ "@a*" Domainname)))))))))))
(assert (not (str.in.re Domainname (re.union (re.* re.allchar) (str.to.re ".7")))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> 0 0))
(assert (<= (str.len Domainname) 256))
(assert (> (str.len Username) (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.++ (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ re.allchar (re.* (str.to.re "."))))) (re.+ (re.+ (re.union (re.range "A" "Z") (str.to.re "."))))) (str.to.re "@"))))))))
(assert (not (str.in.re Domainname (re.++ (re.* (re.union (re.range "0" "a") (re.range (str.++ "@" (str.++ Domainname Username)) "z"))) re.allchar))))
(check-sat)

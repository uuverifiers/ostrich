(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= 0 256))
(assert (> (str.len Domainname) (str.len Domainname)))
(assert (= Email "Z"))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.++ (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (str.to.re "e"))) (re.+ (re.+ (re.union (re.range "A" (str.++ "M" (str.++ Domainname Username))) (re.union (re.range "0" "9") (re.range "a" "z")))))) (str.to.re "M"))))))))
(assert (not (str.in.re Domainname (re.++ (re.* (re.++ (re.* (str.to.re "e")) re.allchar)) re.allchar))))
(check-sat)

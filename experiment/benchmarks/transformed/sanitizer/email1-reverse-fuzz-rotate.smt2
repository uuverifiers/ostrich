(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 75))
(assert (> (str.to.int Username) 0))
(assert (<= (str.to.int Domainname) 339))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ (str.++ "@" Username) Domainname)))
(assert (str.in.re Email (re.++ (re.++ (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.* (re.union (re.union (re.range "A" "Z") (re.++ (re.range "0" "9") (re.range "a" "z"))) (re.* (str.to.re ".")))))) (re.++ (str.to.re "@") (re.union (re.range "A" "Z") (re.* (re.++ (re.range "A" "Z") (re.union (re.range "0" "9") (re.range "a" "z")))))))))
(assert (str.in.re Domainname (re.union (re.++ (re.++ (re.++ (str.to.re "6") (re.++ (re.range "0" "9") (re.++ (re.union (re.range "0" "9") (re.opt (re.union (str.to.re "*") (str.to.re "1")))) (re.opt (re.range "0" "9"))))) (re.++ (re.union (re.union (re.range "0" "5") (str.to.re "")) (re.++ (re.union (re.union (re.union (str.to.re "|2") (re.++ (re.range "0" "4") (re.union (str.to.re "2") (re.union (re.range "0" "9") (re.++ (re.union (re.range "0" "9") (re.opt (re.++ (str.to.re "") (str.to.re "B")))) (re.opt (re.range "0" "9"))))))) (re.range "0" "5")) (re.++ (str.to.re "52") (str.to.re "."))) (re.++ (re.++ (str.to.re "O") (re.++ (re.range "0" "4") (re.++ (str.to.re "g") (re.++ (re.range "0" "9") (re.union (re.union (re.range "0" "9") (re.opt (re.++ (str.to.re "0") (str.to.re "1")))) (re.opt (re.range "0" "9"))))))) (re.range "0" "5")))) (str.to.re "."))) (re.union (re.union (str.to.re "%2") (re.++ (re.range "0" "4") (re.++ (str.to.re "2") (re.++ (re.range "0" "9") (re.++ (re.++ (re.range "0" "9") (re.opt (re.union (str.to.re "0") (str.to.re "''\r''")))) (re.opt (re.range "0" "9"))))))) (re.range "0" "5"))) (re.range "0" "4"))))
(check-sat)

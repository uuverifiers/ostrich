(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 64 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.++ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range (str.++ Domainname "@") "Z") (str.to.re ".")))) (re.+ (re.++ (re.++ (re.* re.allchar) (re.range "0" (str.++ "A" Username))) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9"))))))) (str.to.re "@")) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))))))
(assert (not (str.in.re Domainname (re.++ (str.to.re ".") (re.* re.allchar)))))
(check-sat)

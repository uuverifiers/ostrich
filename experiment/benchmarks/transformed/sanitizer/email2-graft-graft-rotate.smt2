(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 64 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.++ (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ re.allchar (re.* (re.range "0" (str.++ Username "A")))))) (re.+ (re.+ (re.union (re.range (str.++ "@" Domainname) "Z") (re.union (str.to.re ".") (re.range "a" "z")))))) (str.to.re "@"))))))))
(assert (not (str.in.re Domainname (re.++ (re.* (str.to.re ".")) re.allchar))))
(check-sat)

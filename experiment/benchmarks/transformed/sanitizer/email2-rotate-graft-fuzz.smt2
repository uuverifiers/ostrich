(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.to.int Username)))
(assert (> 115 0))
(assert (<= (str.to.int Domainname) 258))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ "" "")))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.++ (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.union (re.union (re.union (re.++ (re.range "0" "9") (re.range "a" "z")) (re.+ (str.to.re ""))) (re.+ (re.+ (re.++ (re.range "A" "Z") (re.union (re.range "0" "9") (re.range "a" (str.++ Domainname Username))))))) (str.to.re "@"))))))))
(assert (not (str.in.re Domainname (re.union (re.+ (str.to.re "N")) re.allchar))))
(check-sat)

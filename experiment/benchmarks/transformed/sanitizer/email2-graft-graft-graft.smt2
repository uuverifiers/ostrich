(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> (str.len Domainname) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 64))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "@") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (re.range "0" "a") (re.* re.allchar)))) (re.+ (re.union (re.range (str.++ Username "A") "z") (re.union (re.range (str.++ "@" Domainname) "Z") (re.++ (re.* re.allchar) (str.to.re "."))))))))))
(assert (not (str.in.re Domainname (str.to.re "."))))
(check-sat)

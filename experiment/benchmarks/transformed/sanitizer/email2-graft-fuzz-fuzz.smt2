(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 181))
(assert (> (str.to.int Username) 0))
(assert (<= (str.len Domainname) 854))
(assert (> 0 (str.len Domainname)))
(assert (= Email "?"))
(assert (str.in.re Email (re.++ (re.+ (re.++ (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "G") (re.union (re.+ (re.++ (re.* (re.++ (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "") (re.+ re.allchar)))) (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" (str.++ Username (str.++ "@" Domainname)))))))))))
(assert (not (str.in.re Domainname (re.union (re.+ re.allchar) (str.to.re "")))))
(check-sat)

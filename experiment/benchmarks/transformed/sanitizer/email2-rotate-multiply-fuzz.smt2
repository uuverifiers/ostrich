(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 44))
(assert (> (str.len Username) 0))
(assert (<= (str.to.int Domainname) 822))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ "@Ab" (str.++ Domainname Username))))
(assert (str.in.re Email (re.union (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.* (re.++ (re.range "A" "Z") (re.++ (re.union (re.++ (re.++ (re.range "0" "9") (re.range "a" "z")) (re.* (str.to.re "x."))) (re.* (re.+ (re.union (re.range "A" "Z") (re.++ (re.range "0" "9") (re.range "a" "z")))))) (str.to.re "v@"))))))))
(assert (not (str.in.re Domainname (re.union (re.* (re.++ re.allchar (re.* (str.to.re "s.")))) re.allchar))))
(check-sat)

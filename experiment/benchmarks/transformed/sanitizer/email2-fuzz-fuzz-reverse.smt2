(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 32))
(assert (> (str.to.int Username) 0))
(assert (<= (str.to.int Domainname) 325))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ (str.++ Domainname "") Username)))
(assert (str.in.re Email (re.++ (re.++ (re.++ (re.+ (re.++ (re.++ (re.range "0" "9") (re.range "A" "Z")) (re.range "a" "z"))) (re.+ (re.++ (str.to.re "") (re.* (re.++ (re.union (re.range "A" "Z") (re.range "0" "9")) (re.range "a" "z")))))) (str.to.re "&")) (re.* (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))))))
(assert (not (str.in.re Domainname (re.union (re.* re.allchar) (re.union (str.to.re "i") (re.* re.allchar))))))
(check-sat)

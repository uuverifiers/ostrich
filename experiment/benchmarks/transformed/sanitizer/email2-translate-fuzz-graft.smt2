(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 11))
(assert (> 0 0))
(assert (<= (str.len Domainname) 321))
(assert (> (str.len Domainname) (str.len Username)))
(assert (= Email (str.++ Username "Z")))
(assert (str.in.re Email (re.++ (re.* (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "") (re.union (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re ""))) (re.* (re.++ (re.range "a" "z") (re.++ (re.range "A" (str.++ "" Domainname)) (re.range "0" "9")))))))))
(assert (not (str.in.re Domainname (re.++ (re.+ re.allchar) (str.to.re "j")))))
(check-sat)

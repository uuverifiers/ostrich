(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) (str.len Domainname)))
(assert (> 256 0))
(assert (= Email "z"))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.++ (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.* (str.to.re "p")))) (re.+ (re.+ (re.union (re.range "A" "Z") (re.union (re.range "0" "9") (re.range "a" (str.++ "H" (str.++ Domainname Username)))))))) (str.to.re "H"))))))))
(assert (not (str.in.re Domainname (re.++ (re.* (re.++ re.allchar (str.to.re "p"))) re.allchar))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> (str.len Domainname) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 64 0))
(assert (= Email (str.++ "@" "z")))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.++ (re.range "A" "Z") (re.++ (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.range "a" "A"))) (re.+ (re.+ (re.union (re.range (str.++ Domainname Username) "Z") (re.union (re.range "0" "9") (str.to.re ".")))))) (str.to.re "@"))))))))
(assert (not (str.in.re Domainname (re.++ (re.* (str.to.re ".")) re.allchar))))
(check-sat)

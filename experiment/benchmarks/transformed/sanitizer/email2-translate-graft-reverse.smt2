(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= 0 256))
(assert (> (str.len Domainname) (str.len Domainname)))
(assert (= Email "Z"))
(assert (str.in.re Email (re.++ (re.++ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" (str.++ (str.++ Domainname "M") Username)) (re.range "0" "9")))) (re.+ (re.++ (str.to.re "e") (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9"))))))) (str.to.re "M")) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))))))
(assert (not (str.in.re Domainname (re.++ (re.++ (str.to.re "e") (re.* re.allchar)) (re.* re.allchar)))))
(check-sat)

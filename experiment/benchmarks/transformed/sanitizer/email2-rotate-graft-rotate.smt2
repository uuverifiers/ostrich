(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 64 0))
(assert (<= (str.len Domainname) 256))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ "@" "z")))
(assert (str.in.re Email (re.++ (re.union (re.range "a" "z") (re.+ (re.++ (re.++ (re.union (re.+ (str.to.re ".")) (re.union (re.range "0" "9") (re.+ (re.+ (re.union (re.range "0" "9") (re.union (re.range "a" (str.++ Domainname Username)) (re.range "A" "Z"))))))) (str.to.re "@")) (re.++ (re.range "a" "z") (re.range "A" "Z"))))) (re.union (re.range "0" "9") (re.range "A" "Z")))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (str.to.re ".")))))
(check-sat)

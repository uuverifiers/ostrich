(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 101))
(assert (> (str.len Username) 0))
(assert (<= 0 214))
(assert (> (str.to.int Domainname) (str.to.int Domainname)))
(assert (= Email ""))
(assert (str.in.re Email (re.++ (re.* (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "#") (re.union (re.* (re.++ (re.* (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "<"))) (re.* (re.union (re.range "a" "z") (re.++ (re.range "A" (str.++ Username (str.++ "" Domainname))) (re.range "0" "9")))))))))
(assert (not (str.in.re Domainname (re.union (re.* re.allchar) (re.union (re.+ re.allchar) (str.to.re "L"))))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 64 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "m") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (re.range "0" (str.++ Username "/")) (re.* re.allchar)))) (re.+ (re.union (re.range "a" "z") (re.union (re.range (str.++ "m" Domainname) "Z") (str.to.re "b")))))))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (str.to.re "b")))))
(check-sat)

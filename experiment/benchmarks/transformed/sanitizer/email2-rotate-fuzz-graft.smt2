(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Domainname)))
(assert (> (str.len Username) 0))
(assert (<= 1 509))
(assert (> (str.to.int Domainname) 0))
(assert (= Email "z"))
(assert (str.in.re Email (re.union (re.range "A" "Z") (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.union (re.range "A" "Z") (re.union (re.union (re.++ (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.* (str.to.re "{")))) (re.+ (re.+ (re.union (re.range "A" "Z") (re.++ (re.range "0" "9") (re.range "a" (str.++ "" (str.++ Domainname Username)))))))) (str.to.re ""))))))))
(assert (not (str.in.re Domainname (re.union (re.+ (re.union re.allchar (str.to.re "."))) re.allchar))))
(check-sat)

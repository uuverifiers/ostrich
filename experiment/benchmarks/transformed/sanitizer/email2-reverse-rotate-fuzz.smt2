(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 4))
(assert (> (str.len Username) 0))
(assert (<= (str.to.int Domainname) 23))
(assert (> (str.to.int Domainname) 0))
(assert (= Email (str.++ (str.++ "" Username) Domainname)))
(assert (str.in.re Email (re.++ (re.union (re.union (re.range "0" "9") (re.range "a" "z")) (re.+ (re.* (re.union (re.union (re.range "A" "Z") (re.union (re.range "0" "9") (re.range "a" "z"))) (re.* (str.to.re ".")))))) (re.union (str.to.re "@") (re.union (re.range "A" "Z") (re.+ (re.union (re.range "A" "Z") (re.union (re.range "0" "9") (re.range "a" "z")))))))))
(assert (not (str.in.re Domainname (re.union (str.to.re ";") (re.+ (re.++ re.allchar (re.+ re.allchar)))))))
(check-sat)

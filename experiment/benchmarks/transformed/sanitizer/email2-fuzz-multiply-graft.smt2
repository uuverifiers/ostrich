(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 54))
(assert (> (str.to.int Username) 0))
(assert (<= 936 (str.to.int Domainname)))
(assert (> (str.to.int Domainname) 0))
(assert (= Email (str.++ Username "0")))
(assert (str.in.re Email (re.union (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "@@") (re.union (re.* (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "88"))) (re.* (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range (str.++ "" Domainname) "9")))))))))
(assert (not (str.in.re Domainname (re.union (re.+ re.allchar) (re.++ (re.* re.allchar) (str.to.re ".."))))))
(check-sat)

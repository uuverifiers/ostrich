(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= 0 128))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 512))
(assert (> (str.len Username) (str.len Domainname)))
(assert (= Email "99"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "@@") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "..") (re.* re.allchar)))) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" (str.++ Username (str.++ "@@" Domainname))) (re.range "0" "Z")))))))))
(assert (not (str.in.re Domainname (re.++ (str.to.re "..") (re.* re.allchar)))))
(check-sat)

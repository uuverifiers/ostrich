(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 256))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 1024))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ Username (str.++ "@@@@" Domainname))))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "@@@@") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "...."))) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))))))))
(assert (str.in.re Domainname (re.++ (re.union (re.++ (str.to.re "22225555") (re.range "0" "5")) (re.union (re.++ (str.to.re "2222") (re.++ (re.range "0" "4") (re.range "0" "9"))) (re.++ (re.opt (re.union (str.to.re "0000") (str.to.re "1111"))) (re.++ (re.range "0" "9") (re.opt (re.range "0" "9")))))) (re.++ (str.to.re "....") (re.++ (re.union (re.++ (str.to.re "22225555") (re.range "0" "5")) (re.union (re.++ (str.to.re "2222") (re.++ (re.range "0" "4") (re.range "0" "9"))) (re.++ (re.opt (re.union (str.to.re "0000") (str.to.re "1111"))) (re.++ (re.range "0" "9") (re.opt (re.range "0" "9")))))) (re.++ (str.to.re "....") (re.++ (re.union (re.++ (str.to.re "22225555") (re.range "0" "5")) (re.union (re.++ (str.to.re "2222") (re.++ (re.range "0" "4") (re.range "0" "9"))) (re.++ (re.opt (re.union (str.to.re "0000") (str.to.re "1111"))) (re.++ (re.range "0" "9") (re.opt (re.range "0" "9")))))) (re.++ (str.to.re "....") (re.union (re.++ (str.to.re "22225555") (re.range "0" "5")) (re.union (re.++ (str.to.re "2222") (re.++ (re.range "0" "4") (re.range "0" "9"))) (re.++ (re.opt (re.union (str.to.re "0000") (str.to.re "1111"))) (re.++ (re.range "0" "9") (re.opt (re.range "0" "9"))))))))))))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "O") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "S"))) (re.+ (re.union (re.range "a" "z") (re.union (str.to.re "S") (re.range "0" (str.++ Username (str.++ "O" Domainname)))))))))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (re.++ (re.range "A" "Z") (re.* re.allchar))))))
(check-sat)

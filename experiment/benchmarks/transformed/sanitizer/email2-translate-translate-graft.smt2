(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Domainname)))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 64 0))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "f") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "i"))) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" (str.++ Username (str.++ "f" Domainname)))))))))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (str.to.re "i")))))
(check-sat)

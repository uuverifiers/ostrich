(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 14))
(assert (> (str.to.int Username) 0))
(assert (<= (str.to.int Domainname) 169))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.range "A" "Z") (re.++ (re.++ (re.range "0" "9") (re.range "a" "z")) (re.* (re.++ (re.range "A" "Z") (re.++ (re.union (re.++ (re.++ (re.range "0" "9") (re.range "a" "z")) (re.* (re.union re.allchar (re.* (str.to.re ""))))) (re.+ (re.* (re.++ (re.range "A" "Z") (re.++ (re.range "0" (str.++ "@" (str.++ Domainname Username))) (re.range "a" "z")))))) (str.to.re ""))))))))
(assert (not (str.in.re Domainname (re.union (re.+ (str.to.re ".")) re.allchar))))
(check-sat)

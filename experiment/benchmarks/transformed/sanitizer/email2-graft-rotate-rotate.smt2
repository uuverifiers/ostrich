(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= (str.len Domainname) 256))
(assert (> 0 (str.len Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.++ (re.union (re.range "a" "z") (re.+ (re.++ (re.++ (re.union (re.+ (re.++ (str.to.re ".") (re.* re.allchar))) (re.union (re.range "0" "9") (re.+ (re.+ (re.union (re.range "0" (str.++ Domainname (str.++ Username "@"))) (re.union (re.range "a" "z") (re.range "A" "Z"))))))) (str.to.re "@")) (re.++ (re.range "a" "z") (re.range "A" "Z"))))) (re.union (re.range "0" "9") (re.range "A" "Z")))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (str.to.re ".")))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 64))
(assert (> (str.len Username) 0))
(assert (<= 0 256))
(assert (> (str.len Domainname) (str.len Domainname)))
(assert (= Email "-"))
(assert (str.in.re Email (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (str.to.re "n") (re.++ (re.+ (re.++ (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "j"))) (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" (str.++ Username (str.++ "n" Domainname))) (re.range "0" "9")))))))))
(assert (not (str.in.re Domainname (re.++ (re.* re.allchar) (re.++ (re.* re.allchar) (str.to.re "j"))))))
(check-sat)

(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) 142))
(assert (> (str.to.int Username) 0))
(assert (<= (str.to.int Domainname) 844))
(assert (> (str.len Domainname) 0))
(assert (= Email (str.++ (str.++ Domainname "_j") Username)))
(assert (str.in.re Email (re.union (re.++ (re.++ (re.* (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.* (re.++ (str.to.re ".' '") (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9"))))))) (str.to.re "`+s""")) (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))))))
(assert (not (str.in.re Domainname (re.union (re.union (re.+ re.allchar) (str.to.re "..")) (re.+ re.allchar)))))
(check-sat)

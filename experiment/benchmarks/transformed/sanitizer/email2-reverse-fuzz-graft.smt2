(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.to.int Username) 11))
(assert (> (str.len Username) 0))
(assert (<= (str.to.int Domainname) 46))
(assert (> 0 (str.to.int Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.union (re.union (re.union (re.* (re.++ (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.+ (re.++ (str.to.re ".") (re.+ (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9"))))))) (re.+ re.allchar)) (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" (str.++ (str.++ Domainname "") Username))))))))
(assert (not (str.in.re Domainname (re.++ (re.++ (str.to.re "h") (str.to.re "w")) (re.+ re.allchar)))))
(check-sat)

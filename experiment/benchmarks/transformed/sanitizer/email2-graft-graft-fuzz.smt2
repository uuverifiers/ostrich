(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 88 0))
(assert (<= (str.to.int Domainname) 322))
(assert (> 0 (str.to.int Domainname)))
(assert (= Email "9"))
(assert (str.in.re Email (re.union (re.+ (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "") (re.++ (re.* (re.union (re.* (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.range "0" "9")))) (re.++ (re.range "0" (str.++ Username "'\x0c'")) (re.* re.allchar)))) (re.* (re.union (re.range "a" "z") (re.union (re.range (str.++ "@" Domainname) "Z") (str.to.re "'\t'")))))))))
(assert (not (str.in.re Domainname (re.union (re.+ re.allchar) (str.to.re ".")))))
(check-sat)

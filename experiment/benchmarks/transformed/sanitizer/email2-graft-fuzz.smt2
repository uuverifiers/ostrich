(declare-const Username String)
(declare-const Domainname String)
(declare-const Email String)
(assert (<= (str.len Username) (str.len Username)))
(assert (> 47 0))
(assert (<= (str.to.int Domainname) 146))
(assert (> (str.to.int Domainname) 0))
(assert (= Email "0"))
(assert (str.in.re Email (re.union (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (re.union (str.to.re "6") (re.++ (re.+ (re.union (re.+ (re.union (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range "0" "9")))) (str.to.re "."))) (re.+ (re.++ (re.range "a" "z") (re.++ (re.range "A" "Z") (re.range (str.++ Username (str.++ "q" Domainname)) "9")))))))))
(assert (not (str.in.re Domainname (re.++ (re.+ re.allchar) (re.union (re.+ re.allchar) (str.to.re ""))))))
(check-sat)

;test regex dat$Col3 <- gsub('(^\\w+).*, ([0-9]{4}).*','\\1 \\2',dat$Col3)
(declare-const X String)
(assert (str.in_re X (re.++ (re.++ (str.to_re "d") (re.++ (str.to_re "a") (str.to_re "t"))) (re.++ (str.to_re "") (re.++ (str.to_re "C") (re.++ (str.to_re "o") (re.++ (str.to_re "l") (re.++ (str.to_re "3") (re.++ (str.to_re " ") (re.++ (str.to_re "<") (re.++ (str.to_re "-") (re.++ (str.to_re " ") (re.++ (str.to_re "g") (re.++ (str.to_re "s") (re.++ (str.to_re "u") (re.++ (str.to_re "b") (re.++ (re.++ (re.++ (re.++ (re.++ (str.to_re "\u{27}") (re.++ (re.++ (str.to_re "") (re.++ (str.to_re "\\") (re.+ (str.to_re "w")))) (re.* (re.diff re.allchar (str.to_re "\n"))))) (re.++ (str.to_re ",") (re.++ (str.to_re " ") (re.++ ((_ re.loop 4 4) (re.range "0" "9")) (re.++ (re.* (re.diff re.allchar (str.to_re "\n"))) (str.to_re "\u{27}")))))) (re.++ (str.to_re ",") (re.++ (str.to_re "\u{27}") (re.++ (str.to_re "\\") (re.++ (str.to_re "1") (re.++ (str.to_re " ") (re.++ (str.to_re "\\") (re.++ (str.to_re "2") (str.to_re "\u{27}"))))))))) (re.++ (str.to_re ",") (re.++ (str.to_re "d") (re.++ (str.to_re "a") (str.to_re "t"))))) (re.++ (str.to_re "") (re.++ (str.to_re "C") (re.++ (str.to_re "o") (re.++ (str.to_re "l") (str.to_re "3"))))))))))))))))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
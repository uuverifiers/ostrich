;test regex my $regex='(((int|long|double|float|void)\s*?\w{1,25})|if|while|for)';
(declare-const X String)
(assert (str.in_re X (re.++ (re.++ (str.to_re "m") (re.++ (str.to_re "y") (str.to_re " "))) (re.++ (str.to_re "") (re.++ (str.to_re "r") (re.++ (str.to_re "e") (re.++ (str.to_re "g") (re.++ (str.to_re "e") (re.++ (str.to_re "x") (re.++ (str.to_re "=") (re.++ (str.to_re "\u{27}") (re.++ (re.union (re.union (re.union (re.++ (re.union (re.union (re.union (re.union (re.++ (str.to_re "i") (re.++ (str.to_re "n") (str.to_re "t"))) (re.++ (str.to_re "l") (re.++ (str.to_re "o") (re.++ (str.to_re "n") (str.to_re "g"))))) (re.++ (str.to_re "d") (re.++ (str.to_re "o") (re.++ (str.to_re "u") (re.++ (str.to_re "b") (re.++ (str.to_re "l") (str.to_re "e"))))))) (re.++ (str.to_re "f") (re.++ (str.to_re "l") (re.++ (str.to_re "o") (re.++ (str.to_re "a") (str.to_re "t")))))) (re.++ (str.to_re "v") (re.++ (str.to_re "o") (re.++ (str.to_re "i") (str.to_re "d"))))) (re.++ (re.* (re.union (str.to_re " ") (re.union (str.to_re "\u{0b}") (re.union (str.to_re "\u{0a}") (re.union (str.to_re "\u{0d}") (re.union (str.to_re "\u{09}") (str.to_re "\u{0c}"))))))) ((_ re.loop 1 25) (re.union (re.range "a" "z") (re.union (re.range "A" "Z") (re.union (re.range "0" "9") (str.to_re "_"))))))) (re.++ (str.to_re "i") (str.to_re "f"))) (re.++ (str.to_re "w") (re.++ (str.to_re "h") (re.++ (str.to_re "i") (re.++ (str.to_re "l") (str.to_re "e")))))) (re.++ (str.to_re "f") (re.++ (str.to_re "o") (str.to_re "r")))) (re.++ (str.to_re "\u{27}") (str.to_re ";"))))))))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
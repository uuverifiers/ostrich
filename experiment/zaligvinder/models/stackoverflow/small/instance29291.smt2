;test regex .*(daughter|ds|son|dd|)(is|was|is turning|turned)([0-9]{2}).*
(declare-const X String)
(assert (str.in_re X (re.++ (re.* (re.diff re.allchar (str.to_re "\n"))) (re.++ (re.union (re.++ (str.to_re "") (re.union (re.union (re.union (re.++ (str.to_re "d") (re.++ (str.to_re "a") (re.++ (str.to_re "u") (re.++ (str.to_re "g") (re.++ (str.to_re "h") (re.++ (str.to_re "t") (re.++ (str.to_re "e") (str.to_re "r")))))))) (re.++ (str.to_re "d") (str.to_re "s"))) (re.++ (str.to_re "s") (re.++ (str.to_re "o") (str.to_re "n")))) (re.++ (str.to_re "d") (str.to_re "d")))) (str.to_re "")) (re.++ (re.union (re.union (re.union (re.++ (str.to_re "i") (str.to_re "s")) (re.++ (str.to_re "w") (re.++ (str.to_re "a") (str.to_re "s")))) (re.++ (str.to_re "i") (re.++ (str.to_re "s") (re.++ (str.to_re " ") (re.++ (str.to_re "t") (re.++ (str.to_re "u") (re.++ (str.to_re "r") (re.++ (str.to_re "n") (re.++ (str.to_re "i") (re.++ (str.to_re "n") (str.to_re "g"))))))))))) (re.++ (str.to_re "t") (re.++ (str.to_re "u") (re.++ (str.to_re "r") (re.++ (str.to_re "n") (re.++ (str.to_re "e") (str.to_re "d"))))))) (re.++ ((_ re.loop 2 2) (re.range "0" "9")) (re.* (re.diff re.allchar (str.to_re "\n")))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
;test regex &sRegex = "/(ps[cp])/([^\/_]*)?(?:_newwin)?/([^\/]*)?/([^\/]*)?/([csehtqwnf]{1})/";
(declare-const X String)
(assert (str.in_re X (re.++ (str.to_re "&") (re.++ (str.to_re "s") (re.++ (str.to_re "R") (re.++ (str.to_re "e") (re.++ (str.to_re "g") (re.++ (str.to_re "e") (re.++ (str.to_re "x") (re.++ (str.to_re " ") (re.++ (str.to_re "=") (re.++ (str.to_re " ") (re.++ (str.to_re "\u{22}") (re.++ (str.to_re "/") (re.++ (re.++ (str.to_re "p") (re.++ (str.to_re "s") (re.union (str.to_re "c") (str.to_re "p")))) (re.++ (str.to_re "/") (re.++ (re.opt (re.* (re.inter (re.diff re.allchar (str.to_re "/")) (re.diff re.allchar (str.to_re "_"))))) (re.++ (re.opt (re.++ (str.to_re "_") (re.++ (str.to_re "n") (re.++ (str.to_re "e") (re.++ (str.to_re "w") (re.++ (str.to_re "w") (re.++ (str.to_re "i") (str.to_re "n")))))))) (re.++ (str.to_re "/") (re.++ (re.opt (re.* (re.diff re.allchar (str.to_re "/")))) (re.++ (str.to_re "/") (re.++ (re.opt (re.* (re.diff re.allchar (str.to_re "/")))) (re.++ (str.to_re "/") (re.++ ((_ re.loop 1 1) (re.union (str.to_re "c") (re.union (str.to_re "s") (re.union (str.to_re "e") (re.union (str.to_re "h") (re.union (str.to_re "t") (re.union (str.to_re "q") (re.union (str.to_re "w") (re.union (str.to_re "n") (str.to_re "f")))))))))) (re.++ (str.to_re "/") (re.++ (str.to_re "\u{22}") (str.to_re ";")))))))))))))))))))))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
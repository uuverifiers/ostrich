;test regex gsub("\\s*(\\d{4}|\\bNV\\b)\\s*","#-#\\1#-#", wines$wine)
(declare-const X String)
(assert (str.in_re X (re.++ (str.to_re "g") (re.++ (str.to_re "s") (re.++ (str.to_re "u") (re.++ (str.to_re "b") (re.++ (re.++ (re.++ (re.++ (str.to_re "\u{22}") (re.++ (str.to_re "\\") (re.++ (re.* (str.to_re "s")) (re.++ (re.union (re.++ (str.to_re "\\") ((_ re.loop 4 4) (str.to_re "d"))) (re.++ (str.to_re "\\") (re.++ (str.to_re "b") (re.++ (str.to_re "N") (re.++ (str.to_re "V") (re.++ (str.to_re "\\") (str.to_re "b"))))))) (re.++ (str.to_re "\\") (re.++ (re.* (str.to_re "s")) (str.to_re "\u{22}"))))))) (re.++ (str.to_re ",") (re.++ (str.to_re "\u{22}") (re.++ (str.to_re "#") (re.++ (str.to_re "-") (re.++ (str.to_re "#") (re.++ (str.to_re "\\") (re.++ (str.to_re "1") (re.++ (str.to_re "#") (re.++ (str.to_re "-") (re.++ (str.to_re "#") (str.to_re "\u{22}")))))))))))) (re.++ (str.to_re ",") (re.++ (str.to_re " ") (re.++ (str.to_re "w") (re.++ (str.to_re "i") (re.++ (str.to_re "n") (re.++ (str.to_re "e") (str.to_re "s")))))))) (re.++ (str.to_re "") (re.++ (str.to_re "w") (re.++ (str.to_re "i") (re.++ (str.to_re "n") (str.to_re "e"))))))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
;test regex %^\d{1,2}:\d{1,2}([ap])m\[(Mon|Tue|Wed|Thu|Fri|-){1,} Only\]$%mg
(declare-const X String)
(assert (str.in_re X (re.++ (re.++ (str.to_re "%") (re.++ (str.to_re "") (re.++ ((_ re.loop 1 2) (re.range "0" "9")) (re.++ (str.to_re ":") (re.++ ((_ re.loop 1 2) (re.range "0" "9")) (re.++ (re.union (str.to_re "a") (str.to_re "p")) (re.++ (str.to_re "m") (re.++ (str.to_re "[") (re.++ (re.++ (re.* (re.union (re.union (re.union (re.union (re.union (re.++ (str.to_re "M") (re.++ (str.to_re "o") (str.to_re "n"))) (re.++ (str.to_re "T") (re.++ (str.to_re "u") (str.to_re "e")))) (re.++ (str.to_re "W") (re.++ (str.to_re "e") (str.to_re "d")))) (re.++ (str.to_re "T") (re.++ (str.to_re "h") (str.to_re "u")))) (re.++ (str.to_re "F") (re.++ (str.to_re "r") (str.to_re "i")))) (str.to_re "-"))) ((_ re.loop 1 1) (re.union (re.union (re.union (re.union (re.union (re.++ (str.to_re "M") (re.++ (str.to_re "o") (str.to_re "n"))) (re.++ (str.to_re "T") (re.++ (str.to_re "u") (str.to_re "e")))) (re.++ (str.to_re "W") (re.++ (str.to_re "e") (str.to_re "d")))) (re.++ (str.to_re "T") (re.++ (str.to_re "h") (str.to_re "u")))) (re.++ (str.to_re "F") (re.++ (str.to_re "r") (str.to_re "i")))) (str.to_re "-")))) (re.++ (str.to_re " ") (re.++ (str.to_re "O") (re.++ (str.to_re "n") (re.++ (str.to_re "l") (re.++ (str.to_re "y") (str.to_re "]"))))))))))))))) (re.++ (str.to_re "") (re.++ (str.to_re "%") (re.++ (str.to_re "m") (str.to_re "g")))))))
; sanitize danger characters:  < > ' " \ / &
(assert (not (str.in_re X (re.* (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{5c}") (str.to_re "\u{2f}") (str.to_re "\u{26}"))))))
(assert (< 20 (str.len X)))
(check-sat)
(get-model)
;test regex \d{1,5}\ ?\d{0,5}\ ?[a-zA-Z]{1,10}\.?\ ?[a-zA-Z]{0,10}\.?\ ?[a-zA-Z]{1,30}\ ?[a-zA-Z]{2,30}\ ?[a-zA-Z]{2,30}\ ?[a-zA-Z]{0,15}\,?\s?
(declare-const X String)
(assert (str.in_re X (re.++ ((_ re.loop 1 5) (re.range "0" "9")) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 0 5) (re.range "0" "9")) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 1 10) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re ".")) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 0 10) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re ".")) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 1 30) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 2 30) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 2 30) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re " ")) (re.++ ((_ re.loop 0 15) (re.union (re.range "a" "z") (re.range "A" "Z"))) (re.++ (re.opt (str.to_re ",")) (re.opt (re.union (str.to_re " ") (re.union (str.to_re "\u{0b}") (re.union (str.to_re "\u{0a}") (re.union (str.to_re "\u{0d}") (re.union (str.to_re "\u{09}") (str.to_re "\u{0c}")))))))))))))))))))))))))))
; sanitize danger characters:  < > ' " &
(assert (not (str.in_re X (re.++ re.all (re.union (str.to_re "\u{3c}") (str.to_re "\u{3e}") (str.to_re "\u{27}") (str.to_re "\u{22}") (str.to_re "\u{26}")) re.all))))
(assert (< 50 (str.len X)))
(check-sat)
(get-model)